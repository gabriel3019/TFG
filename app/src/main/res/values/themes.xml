<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tfg" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Tfg" parent="Base.Theme.Tfg" />

    <style name="Theme.TextMain" parent="Base.Theme.Tfg" >
        <item name="android:fontFamily">@font/bison</item>
    </style>

    <style name="Theme.Tfg.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item> <!-- Fondo del splash -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_goiko2</item> <!-- Tu logo -->
        <item name="postSplashScreenTheme">@style/Theme.Tfg</item> <!-- Vuelve al tema original -->
    </style>

</resources>