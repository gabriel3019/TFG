<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:datePickerDialogTheme">@style/CustomDatePickerDialog</item>
    </style>

    <style name="Widget.Goiko.Spinner" parent="Widget.AppCompat.Spinner">
        <item name="android:background">#FFFFFF</item>
        <item name="android:popupBackground">#FFFFFF</item>
    </style>

    <style name="CustomDatePickerDialog" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="android:headerBackground">@color/black</item>
        <item name="android:textColorPrimaryInverse">@color/white</item>
        <item name="colorAccent">@color/red</item>
        <item name="android:colorControlActivated">@color/red</item>
        <item name="android:calendarTextColor">@color/white</item>
        <item name="android:weekDayTextAppearance">@style/CalendarWeekdayWhite</item>
    </style>

    <style name="CalendarWeekdayWhite" parent="@android:style/TextAppearance.Material">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>